# sslip.io resolves "3-232-58-53.sslip.io" to 3.232.58.53 automatically -- a free way to
# get a real, publicly-resolvable hostname (which Let's Encrypt requires for a cert)
# without owning a domain. This IP is now an Elastic IP (allocated + associated so it
# survives instance stop/start, e.g. for an instance-type change) -- it should no longer
# change on its own, but if the EIP is ever released/reassigned, update this to match.
3-232-58-53.sslip.io {
    reverse_proxy api:8000
}
