soundfile>=0.12
numpy>=1.26
soxr>=0.3
pydantic>=2.5

# ML: background noise (AST / AudioSet); reused by later model stages
torch>=2.2
transformers>=4.40

# Transcription (self-hosted) + lexical-fusion LLM (emotion stage 5b)
faster-whisper>=1.0
openai>=1.40
python-dotenv>=1.0

# dev
pytest>=8.0
